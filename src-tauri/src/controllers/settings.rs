pub struct Settings {
    // not sure what I can do here? I think this is just a trait to hold all commands?
    // not sure, let's figure this one out later and see what BlendFarm expose for settings?
}
